@model List<ResultCategoryDto>

@{
    ViewData["Title"] = "Kategoriler";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section class="content-header">
    <h1>Kategori işlemleri</h1>
</section>

<div class="chart-box">
    <section class="content-header">
        <h3>Kategori listesi</h3>
        <p>Kategori ile ilgili işlemlerinizi aşağıdaki tablo üzerinden gerçekleştirebilirsiniz.</p>
    </section>

    @if(Model.Count > 0)
    {
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Adı</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr>
                        <td>
                            <p style="padding: 12px 0px;">@category.Name</p>
                        </td>
                        <td>
                            <ul class="sidebar-menu" data-widget="tree">
                                <li class="treeview">
                                    <a href="#"> <span>Seçenekler</span> <span class="pull-right-container"> <i class="fa fa-angle-left pull-right"></i> </span> </a>
                                    <ul class="treeview-menu">
                                        <li><a href="/admin/product/category/@category.Id"><i class="fa fa-angle-right"></i> Kategorilere ait ürünleri getir</a></li>
                                        <li>
                                            <form asp-action="Delete" asp-controller="Category" asp-area="Admin" method="post">
                                                <input type="hidden" name="id" value="@category.Id" />
                                                <button type="submit" class="btn btn-link" style="padding:8px 10px; text-decoration:none; color:#787878"><i class="fa fa-angle-right" style="width:10px; margin-right:6px;"></i>Sil</button>
                                            </form>
                                        </li>
                                        <li>
                                            <a href="/admin/category/update/@category.Id"><i class="fa fa-angle-right"></i>Güncelle</a>
                                        </li>
                                    </ul>
                                </li>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div role="alert" class="alert alert-info">Herhangi bir kategori bulunmamaktadır.</div>
    }

    <a href="/admin/category/create" class="btn btn-primary">Yeni kategori ekle</a>
</div>

