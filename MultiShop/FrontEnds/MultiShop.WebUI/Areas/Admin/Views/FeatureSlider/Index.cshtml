@model List<ResultFeatureSliderDto>

@{
    ViewData["Title"] = "Öne çıkan görseller";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section class="content-header">
    <h1>Öne çıkan görsel işlemleri</h1>
</section>

<div class="chart-box">
    <section class="content-header">
        <h3>Öne çıkan görsellerin listesi</h3>
        <p>Öne çıkan görseller ile ilgili işlemlerinizi aşağıdaki tablo üzerinden gerçekleştirebilirsiniz.</p>
    </section>

    @if (Model.Count > 0)
    {
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Başlık</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var featureSlider in Model)
                {
                    <tr>
                        <td>
                            <p style="padding: 12px 0px;">@featureSlider.Title</p>
                        </td>
                        <td>
                            <ul class="sidebar-menu" data-widget="tree">
                                <li class="treeview">
                                    <a href="#"> <span>Seçenekler</span> <span class="pull-right-container"> <i class="fa fa-angle-left pull-right"></i> </span> </a>
                                    <ul class="treeview-menu">
                                        <li>
                                            <form asp-action="Delete" asp-controller="FeatureSlider" asp-area="Admin" method="post">
                                                <input type="hidden" name="id" value="@featureSlider.Id" />
                                                <button type="submit" class="btn btn-link" style="padding:8px 10px; text-decoration:none; color:#787878"><i class="fa fa-angle-right" style="width:10px; margin-right:6px;"></i>Sil</button>
                                            </form>
                                        </li>
                                        <li>
                                            <a href="/admin/featureSlider/update/@featureSlider.Id"><i class="fa fa-angle-right"></i>Güncelle</a>
                                        </li>
                                    </ul>
                                </li>
                            </ul>
                            </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div role="alert" class="alert alert-info">Herhangi bir öne çıkan görsel bulunmamaktadır.</div>
    }

    <a href="/admin/featureSlider/create" class="btn btn-primary">Yeni öne çıkan görsel ekle</a>
</div>

